# Production API (required on Vercel)
NEXT_PUBLIC_API_BASE_URL=https://coinzy-experts-api.trackzio.com

# Reference only — do NOT add to Vercel env vars.
# Operators enter the admin key on the login page (sessionStorage).
# ADMIN_API_KEY=<your-production-admin-key>
